# Reproducibility Checklist

Before the frozen test campaign record: U-TRUST Git commit; AgentDojo/InjecAgent commits; exact Hugging Face model IDs and revisions; model-access/license status; Python/PyTorch/CUDA/driver/Transformers/bitsandbytes versions; GPU topology and VRAM; quantization and decoding; prompt SHA-256 hashes; benchmark task IDs and split; seeds; calibrator coefficients/intercept; eta and routing thresholds; raw-result schema version.

Synthetic smoke outputs are engineering checks only and must never populate manuscript results.
